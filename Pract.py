# create a dictionary with integer keys
dics = {
    '1': 'parag',
    '2': 'ram',
    '3': 'arnav'
}
# using keys and valus we cereate
keyss = dics.keys()
valuess = dics.values()
print(valuess, keyss)
# print(keyss)
